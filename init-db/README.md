# 数据库初始化脚本

将 SQL 初始化脚本放在此目录下，Docker 容器启动时会自动执行。

## 使用说明

1. 将你的 `kamanote-tech.sql` 文件放到这个目录
2. 文件会按字母顺序执行，建议使用数字前缀命名：
   - `01-schema.sql` - 表结构
   - `02-data.sql` - 初始数据

## 注意事项

- 脚本只会在数据库首次初始化时执行
- 如需重新执行，需要删除 MySQL 数据卷：`docker-compose down -v`
